# Profile Setup

[prev: 04-private-vs-public](./04-private-vs-public.md) | [next: 06-profile-readme](./06-profile-readme.md)
---

## Введение

Профиль GitHub — это ваша профессиональная визитка в мире разработки. Рекрутёры, коллеги и потенциальные работодатели часто смотрят профиль перед собеседованием или приглашением в проект. Правильно настроенный профиль демонстрирует вашу активность, навыки и профессионализм.

---

## 1. Аватар и Bio

### Аватар (Profile Picture)

**Где настроить:**
```
Settings → Public profile → Profile picture → Edit → Upload a photo
```

**Рекомендации:**
```
✓ Профессиональное фото
  - Чёткое изображение лица
  - Нейтральный фон
  - Хорошее освещение

✓ Или качественный аватар
  - Узнаваемый стиль
  - Высокое разрешение (минимум 400x400)
  - Читаемый в маленьком размере

✗ Избегайте
  - Размытые фото
  - Групповые фото
  - Мемы/неуместные изображения
  - Дефолтную иконку (identicon)
```

**Размеры:**
```
Рекомендуемый размер: 400x400 пикселей
Максимальный размер файла: 1 MB
Форматы: PNG, JPG, GIF
```

### Bio (Биография)

**Где настроить:**
```
Settings → Public profile → Bio
```

**Лимит:** 160 символов

**Примеры хороших bio:**
```
Backend developer | Python & Go enthusiast | Building APIs at @company

Full-stack dev | React + Node.js | Open source contributor

CS student at MIT | Interested in ML and distributed systems

DevOps Engineer | Kubernetes, Terraform, AWS | Automating everything
```

**Структура:**
```
[Роль/Должность] | [Технологии/Интересы] | [Текущее место работы/проект]
```

### Name (Отображаемое имя)

```
Settings → Public profile → Name

Варианты:
- Реальное имя: "Ivan Petrov"
- Имя + никнейм: "Ivan 'devmaster' Petrov"
- Только имя: "Ivan"
```

### Pronouns (Местоимения)

```
Settings → Public profile → Pronouns

Опционально:
- he/him
- she/her
- they/them
- Custom
```

### Location (Местоположение)

```
Settings → Public profile → Location

Примеры:
- Moscow, Russia
- Remote
- San Francisco Bay Area
- Earth 🌍 (для космополитов)
```

### Website/URL

```
Settings → Public profile → Website

Примеры:
- Личный сайт/портфолио
- Блог
- LinkedIn профиль
- Twitter
```

### Company

```
Settings → Public profile → Company

Формат:
- @company-github-org  ← линкуется на организацию
- Company Name         ← просто текст
- @company | Freelance ← комбинация
```

---

## 2. Настройки приватности

### Profile Settings

```
Settings → Public profile

Что можно настроить:
☐ Include private contributions on my profile
  → Показывать приватную активность в календаре

☐ Show activity overview
  → Показывать summary активности

☐ Display current local time
  → Показывать вашу таймзону
```

### Activity Privacy

```
Settings → Public profile → Contributions & Activity

Contribution settings:
☑ Make profile private
  → Полностью скрывает активность

☑ Include private contributions
  → Учитывает private repos в графе (без деталей)
```

### Блокировка пользователей

```
Settings → Moderation → Blocked users → Block a user

Заблокированный пользователь:
- Не может follow вас
- Не может watch ваши repos
- Не может комментировать ваши issues
- Вы не видите их активность
```

### Ограничение взаимодействия

```
Settings → Moderation → Interaction limits

Опции:
- Limit to existing users
- Limit to prior contributors
- Limit to repository collaborators

Срок: 24 hours / 3 days / 1 week / 1 month / 6 months
```

---

## 3. SSH ключи

### Зачем нужны SSH ключи

```
SSH ключи позволяют:
- Аутентифицироваться без ввода пароля
- Безопасно подключаться к GitHub
- Push/pull без токенов

Состоит из:
- Private key (id_rsa или id_ed25519) — НИКОГДА не публиковать!
- Public key (id_rsa.pub или id_ed25519.pub) — добавляется на GitHub
```

### Генерация SSH ключа

```bash
# Рекомендуемый алгоритм (Ed25519)
ssh-keygen -t ed25519 -C "your_email@example.com"

# Или RSA (4096 бит), если Ed25519 не поддерживается
ssh-keygen -t rsa -b 4096 -C "your_email@example.com"

# Диалог:
Enter file in which to save the key (/home/user/.ssh/id_ed25519): [Enter]
Enter passphrase (empty for no passphrase): [пароль для ключа]
Enter same passphrase again: [повторить пароль]
```

**Passphrase:**
- Рекомендуется установить для дополнительной защиты
- Можно использовать ssh-agent, чтобы не вводить каждый раз

### Добавление ключа в ssh-agent

```bash
# Запустить ssh-agent
eval "$(ssh-agent -s)"

# macOS: добавить конфигурацию
echo "Host *
  AddKeysToAgent yes
  UseKeychain yes
  IdentityFile ~/.ssh/id_ed25519" >> ~/.ssh/config

# Добавить ключ
ssh-add ~/.ssh/id_ed25519        # Linux
ssh-add --apple-use-keychain ~/.ssh/id_ed25519   # macOS
```

### Копирование публичного ключа

```bash
# macOS
pbcopy < ~/.ssh/id_ed25519.pub

# Linux (xclip)
xclip -selection clipboard < ~/.ssh/id_ed25519.pub

# Linux (xsel)
xsel --clipboard < ~/.ssh/id_ed25519.pub

# Windows (Git Bash)
clip < ~/.ssh/id_ed25519.pub

# Или просто вывести и скопировать вручную
cat ~/.ssh/id_ed25519.pub
```

### Добавление SSH ключа на GitHub

```
Settings → SSH and GPG keys → New SSH key

Title: Descriptive name (e.g., "MacBook Pro Work")
Key type: Authentication Key
Key: [вставить содержимое id_ed25519.pub]

[Add SSH key]
```

### Проверка подключения

```bash
ssh -T git@github.com

# Ожидаемый ответ:
Hi username! You've successfully authenticated, but GitHub does not provide shell access.
```

### Несколько SSH ключей

Если у вас несколько аккаунтов GitHub:

```bash
# ~/.ssh/config

# Личный аккаунт
Host github.com-personal
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519_personal

# Рабочий аккаунт
Host github.com-work
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519_work

# Использование:
git clone git@github.com-personal:user/repo.git
git clone git@github.com-work:company/repo.git
```

---

## 4. Email настройки

### Основные настройки email

```
Settings → Emails
```

### Добавление email

```
Add email address → [ввести email] → Add

После добавления:
- Проверьте почту
- Кликните ссылку подтверждения
```

### Primary email

```
Primary email address: [выбрать из списка]

Primary email используется для:
- Уведомления по умолчанию
- Связывание коммитов с профилем
- Восстановление аккаунта
```

### Приватность email

```
☑ Keep my email addresses private
  → Скрывает email на странице профиля
  → GitHub создаёт no-reply адрес: username@users.noreply.github.com

☑ Block command line pushes that expose my email
  → Блокирует push, если коммит содержит ваш реальный email
```

**Использование noreply email:**
```bash
# Настроить Git использовать noreply адрес
git config --global user.email "username@users.noreply.github.com"

# Для конкретного репозитория
cd repo
git config user.email "username@users.noreply.github.com"
```

### Email для уведомлений

```
Default notifications email: [выбрать]

Custom routing:
- Можно настроить разные email для разных организаций
- Org A → work@example.com
- Org B → freelance@example.com
```

---

## 5. GPG ключи (подпись коммитов)

### Зачем подписывать коммиты

```
Подписанные коммиты:
- Подтверждают, что коммит действительно сделан вами
- Показывают "Verified" badge на GitHub
- Защищают от спуфинга (кто угодно может указать ваш email в коммите)
```

### Генерация GPG ключа

```bash
# Генерация ключа
gpg --full-generate-key

# Выберите:
# (1) RSA and RSA
# 4096 bits
# Key valid for: 0 (never expires) или 1y (1 год)
# Real name: Your Name
# Email: your_email@example.com
# Comment: (optional)
```

### Экспорт публичного ключа

```bash
# Список ключей
gpg --list-secret-keys --keyid-format=long

# Пример вывода:
sec   rsa4096/ABC123DEF456GHI7 2024-01-01 [SC]
      Key fingerprint = ...
uid                 [ultimate] Your Name <email@example.com>
ssb   rsa4096/XYZ789... 2024-01-01 [E]

# Экспорт (используйте ID после "rsa4096/")
gpg --armor --export ABC123DEF456GHI7
```

### Добавление GPG ключа на GitHub

```
Settings → SSH and GPG keys → New GPG key

[Вставить output команды gpg --armor --export]

[Add GPG key]
```

### Настройка Git для подписи

```bash
# Указать ключ для подписи
git config --global user.signingkey ABC123DEF456GHI7

# Автоматически подписывать все коммиты
git config --global commit.gpgsign true

# Автоматически подписывать все теги
git config --global tag.gpgsign true
```

### Проверка подписи

```bash
# Коммит с подписью
git commit -S -m "Signed commit"

# Проверка подписи
git log --show-signature

# На GitHub коммит будет показывать "Verified" ✓
```

---

## 6. Дополнительные настройки профиля

### Status (Статус)

```
На странице профиля: Set status

Опции:
- Emoji + текст (до 80 символов)
- Busy checkbox (показывает, что вы заняты)
- Clear status: Never / in 30 min / in 1 hour / in 4 hours / today / this week

Примеры:
🎯 Focusing on Q4 goals
🏠 Working from home
🌴 On vacation until Jan 15
📚 Learning Rust
```

### Achievements (Достижения)

```
Settings → Public profile → Achievements

☑ Show Achievements on my profile

Доступные достижения:
- Arctic Code Vault Contributor
- Galaxy Brain
- Pull Shark
- YOLO
- Pair Extraordinaire
- Quickdraw
- Starstruck
- и другие...
```

### Sponsorship

```
Settings → Sponsors

Если вы open source контрибутор:
- Можно включить GitHub Sponsors
- Получать финансирование от сообщества
- Настроить уровни спонсорства
```

### Featured Repositories

```
На странице профиля → Customize your pins

Можно закрепить до 6 репозиториев:
- Ваши лучшие проекты
- Активные разработки
- Популярные контрибуции

Показывается:
- Название и описание
- Язык программирования
- Stars и Forks
```

---

## 7. Best Practices

### Профиль для поиска работы

```
✓ Профессиональное фото
✓ Реальное имя
✓ Информативное bio с технологиями
✓ Активная activity graph
✓ Закреплённые качественные проекты
✓ README профиля (см. следующий раздел)
✓ Подписанные коммиты
```

### Профиль для open source

```
✓ Узнаваемый аватар
✓ Никнейм ОК, если известен в сообществе
✓ Bio с проектами и интересами
✓ Контактная информация для связи
✓ Sponsor button (если активный maintainer)
```

### Безопасность

```
✓ Уникальный сложный пароль
✓ 2FA включена
✓ SSH ключи с passphrase
✓ Регулярная ротация ключей
✓ Backup recovery codes
✓ Email подтверждён
✓ Приватность email включена
```

---

## 8. Типичные ошибки

### Ошибка 1: Дефолтный профиль

```
❌ Плохо:
- Identicon вместо аватара
- Пустое bio
- Нет закреплённых репозиториев

✅ Хорошо:
- Заполнить все поля
- Добавить фото/аватар
- Закрепить лучшие проекты
```

### Ошибка 2: Публичный реальный email

```
❌ Плохо:
- Email виден на профиле
- Email в коммитах

✅ Хорошо:
- Включить "Keep email private"
- Использовать noreply email в Git
```

### Ошибка 3: SSH ключ без passphrase

```
❌ Плохо:
- SSH ключ без пароля на shared компьютере

✅ Хорошо:
- Всегда устанавливать passphrase
- Использовать ssh-agent
- На рабочих машинах — отдельные ключи
```

### Ошибка 4: Один ключ везде

```
❌ Плохо:
- Один SSH ключ на всех устройствах
- Один ключ для всех аккаунтов

✅ Хорошо:
- Отдельный ключ для каждого устройства
- Названия ключей отражают устройство
- Можно отозвать ключ устройства при утере
```

---

## Заключение

Настройка профиля GitHub — это инвестиция в вашу профессиональную репутацию:

1. **Заполните базовую информацию** — фото, bio, контакты
2. **Настройте безопасность** — 2FA, SSH ключи, приватность email
3. **Подписывайте коммиты** — GPG ключи показывают серьёзный подход
4. **Закрепите лучшие проекты** — первое, что видят посетители
5. **Создайте Profile README** — следующий раздел!

Хорошо оформленный профиль открывает двери к возможностям в мире разработки.

---
[prev: 04-private-vs-public](./04-private-vs-public.md) | [next: 06-profile-readme](./06-profile-readme.md)