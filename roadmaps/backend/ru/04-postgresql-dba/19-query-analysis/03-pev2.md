# PEV2 - Postgres EXPLAIN Visualizer 2

## Введение

**PEV2** (Postgres EXPLAIN Visualizer 2) - это современный open-source инструмент для визуализации планов выполнения запросов PostgreSQL. Это переработанная версия оригинального PEV с улучшенным интерфейсом и новыми возможностями.

## Основные возможности

- **Графическая визуализация** плана в виде дерева
- **Интерактивный интерфейс** - клик на узлы для детальной информации
- **Локальная работа** - данные не отправляются на сервер
- **Поддержка JSON формата** - более точный парсинг
- **Статистика и метрики** на одном экране
- **Темная и светлая темы**

## Как использовать

### Онлайн-версия

Откройте https://explain.dalibo.com/analyze/new (PEV2 интегрирован в Dalibo) или используйте https://tatiyants.com/pev/

### Локальная установка

```bash
# Через npm
npm install -g pev2

# Или клонирование репозитория
git clone https://github.com/dalibo/pev2.git
cd pev2
npm install
npm run serve
```

### Получение плана в JSON формате

PEV2 лучше всего работает с JSON-форматом:

```sql
EXPLAIN (ANALYZE, COSTS, VERBOSE, BUFFERS, FORMAT JSON)
SELECT
    p.name as product_name,
    c.name as category_name,
    COUNT(oi.id) as times_ordered,
    SUM(oi.quantity * oi.price) as total_revenue
FROM products p
JOIN categories c ON p.category_id = c.id
JOIN order_items oi ON p.id = oi.product_id
JOIN orders o ON oi.order_id = o.id
WHERE o.status = 'completed'
  AND o.created_at >= '2024-01-01'
GROUP BY p.id, p.name, c.id, c.name
ORDER BY total_revenue DESC
LIMIT 20;
```

### Вставка плана

1. Скопируйте JSON-вывод EXPLAIN
2. Вставьте в поле ввода PEV2
3. Нажмите "Analyze" или "Submit"

## Интерфейс PEV2

### Диаграмма плана (Plan Diagram)

Визуальное дерево выполнения запроса:

```
[Limit]
    │
    ▼
[Sort]
    │
    ▼
[HashAggregate]
    │
    ▼
[Hash Join] ◄─── [Hash] ◄─── [Seq Scan: orders]
    │
    ▼
[Hash Join] ◄─── [Hash] ◄─── [Seq Scan: products]
    │
    ▼
[Nested Loop]
    │
    ▼
...
```

### Панель узла (Node Details)

При клике на узел отображается:

| Метрика | Описание |
|---------|----------|
| **Node Type** | Тип операции (Seq Scan, Index Scan, Hash Join и т.д.) |
| **Relation Name** | Имя таблицы или индекса |
| **Startup Cost** | Стоимость до получения первой строки |
| **Total Cost** | Полная стоимость операции |
| **Plan Rows** | Ожидаемое количество строк |
| **Actual Rows** | Реальное количество строк |
| **Actual Time** | Реальное время выполнения |
| **Loops** | Количество итераций |
| **Buffers** | Информация о буферах (shared hit/read/written) |

### Цветовая кодировка

PEV2 использует цвета для индикации проблем:

- 🟢 **Зеленый** - быстрые операции
- 🟡 **Желтый/Оранжевый** - средние по времени
- 🔴 **Красный** - самые медленные узлы (узкие места)

### Панель статистики (Stats)

Общая информация о плане:

```
Planning Time:    0.567 ms
Execution Time:   156.234 ms
Total Rows:       20
Triggers:         0
JIT:              No
```

## Особенности визуализации

### Ширина узлов

Ширина каждого узла пропорциональна его стоимости относительно общей стоимости плана. Это позволяет быстро увидеть самые дорогие операции.

### Потоки данных

Стрелки показывают направление потока данных между узлами. Толщина стрелок может отражать количество передаваемых строк.

### Всплывающие подсказки

При наведении на узел появляется краткая информация:
- Время выполнения
- Количество строк
- Процент от общего времени

## Анализ проблем

### Выявление узких мест

1. **Ищите красные узлы** - они занимают больше всего времени
2. **Сравнивайте Plan Rows и Actual Rows** - большое расхождение указывает на проблемы со статистикой
3. **Обращайте внимание на Seq Scan** - возможно, нужен индекс
4. **Проверяйте loops** - большое количество итераций в Nested Loop может быть проблемой

### Пример проблемного узла

```json
{
  "Node Type": "Seq Scan",
  "Relation Name": "orders",
  "Filter": "(status = 'completed'::text)",
  "Rows Removed by Filter": 850000,
  "Plan Rows": 10000,
  "Actual Rows": 150000,
  "Actual Total Time": 2345.678
}
```

**Проблемы:**
- Seq Scan на большой таблице
- Plan Rows (10000) vs Actual Rows (150000) - 15x расхождение
- 850000 строк отфильтровано

**Решения:**
1. Создать индекс на `status`
2. Обновить статистику: `ANALYZE orders;`

## Сравнение с другими инструментами

| Функция | PEV2 | depesz | Dalibo |
|---------|------|--------|--------|
| Графическая визуализация | Да | Нет | Да |
| Локальная работа | Да | Нет* | Да |
| JSON поддержка | Отличная | Хорошая | Отличная |
| Exclusive time | Да | Да | Да |
| Шаринг планов | Зависит | Да | Да |
| Open Source | Да | Да | Да |

*depesz можно развернуть локально

## Практические советы

### 1. Используйте JSON формат

```sql
EXPLAIN (ANALYZE, BUFFERS, FORMAT JSON) ...
```

JSON содержит больше информации и парсится точнее.

### 2. Всегда включайте BUFFERS

```sql
EXPLAIN (ANALYZE, BUFFERS) ...
```

Показывает использование буферного кэша и дисковые чтения.

### 3. Будьте осторожны с ANALYZE

EXPLAIN ANALYZE **выполняет** запрос. Для INSERT/UPDATE/DELETE используйте транзакцию:

```sql
BEGIN;
EXPLAIN ANALYZE DELETE FROM orders WHERE id = 1;
ROLLBACK;
```

### 4. Сравнивайте планы

Сохраните планы до и после оптимизации для сравнения эффективности.

## Интеграция в рабочий процесс

### VSCode Extensions

Некоторые расширения PostgreSQL для VSCode поддерживают визуализацию EXPLAIN.

### pgAdmin

pgAdmin 4 имеет встроенную визуализацию планов, похожую на PEV2.

### DBeaver

DBeaver также поддерживает графическое отображение планов запросов.

## Ссылки

- [PEV2 на GitHub](https://github.com/dalibo/pev2)
- [Онлайн версия](https://explain.dalibo.com/)
- [Документация PostgreSQL EXPLAIN](https://www.postgresql.org/docs/current/sql-explain.html)
