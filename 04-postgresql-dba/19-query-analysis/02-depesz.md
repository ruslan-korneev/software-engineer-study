# explain.depesz.com - Визуализатор планов EXPLAIN

## Введение

**explain.depesz.com** - это бесплатный онлайн-инструмент для визуализации и анализа планов выполнения запросов PostgreSQL. Создан Хубертом Депешем Лубашко (Hubert depesz Lubaczewski) - известным PostgreSQL-экспертом.

## Почему depesz?

Текстовый вывод EXPLAIN может быть сложен для восприятия, особенно для больших запросов с множеством соединений. depesz преобразует план в удобную таблицу с цветовой индикацией проблемных мест.

### Преимущества инструмента

- **Бесплатный и не требует установки**
- **Цветовая индикация** проблемных узлов
- **Расчет эксклюзивного времени** для каждого узла
- **Возможность делиться планами** по ссылке
- **Поддержка всех форматов** (TEXT, JSON, XML, YAML)

## Как использовать

### Шаг 1: Получите план запроса

Выполните запрос с EXPLAIN ANALYZE в PostgreSQL:

```sql
EXPLAIN (ANALYZE, COSTS, VERBOSE, BUFFERS, FORMAT TEXT)
SELECT c.name, COUNT(o.id) as total_orders, SUM(o.amount) as total_amount
FROM customers c
JOIN orders o ON c.id = o.customer_id
WHERE o.created_at >= '2024-01-01'
GROUP BY c.id, c.name
HAVING SUM(o.amount) > 1000
ORDER BY total_amount DESC
LIMIT 100;
```

### Шаг 2: Скопируйте результат

Скопируйте весь вывод EXPLAIN, включая время планирования и выполнения:

```
Limit  (cost=1500.23..1500.48 rows=100 width=52) (actual time=89.123..89.156 rows=100 loops=1)
  ->  Sort  (cost=1500.23..1502.73 rows=1000 width=52) (actual time=89.120..89.145 rows=100 loops=1)
        Sort Key: (sum(o.amount)) DESC
        Sort Method: top-N heapsort  Memory: 32kB
        ->  HashAggregate  (cost=1450.00..1472.50 rows=1000 width=52) (actual time=85.234..87.567 rows=2500 loops=1)
              Group Key: c.id
              Filter: (sum(o.amount) > '1000'::numeric)
              Batches: 1  Memory Usage: 512kB
              Rows Removed by Filter: 1500
              ->  Hash Join  (cost=125.00..1350.00 rows=20000 width=20) (actual time=2.345..65.789 rows=45000 loops=1)
                    Hash Cond: (o.customer_id = c.id)
                    ->  Seq Scan on orders o  (cost=0.00..1100.00 rows=20000 width=16) (actual time=0.015..35.234 rows=45000 loops=1)
                          Filter: (created_at >= '2024-01-01'::date)
                          Rows Removed by Filter: 55000
                    ->  Hash  (cost=100.00..100.00 rows=2000 width=12) (actual time=2.123..2.124 rows=2000 loops=1)
                          Buckets: 2048  Batches: 1  Memory Usage: 96kB
                          ->  Seq Scan on customers c  (cost=0.00..100.00 rows=2000 width=12) (actual time=0.008..1.234 rows=2000 loops=1)
Planning Time: 0.567 ms
Execution Time: 89.456 ms
```

### Шаг 3: Вставьте в depesz

1. Откройте https://explain.depesz.com/
2. Вставьте план в текстовое поле
3. Опционально добавьте название плана
4. Нажмите "Submit"

## Интерпретация результатов

### Колонки таблицы

| Колонка | Описание |
|---------|----------|
| **exclusive** | Время, потраченное только этим узлом (без дочерних) |
| **inclusive** | Общее время узла (включая дочерние) |
| **rows x** | Отношение actual rows / estimated rows |
| **rows** | Количество обработанных строк |
| **loops** | Количество итераций узла |

### Цветовая индикация

depesz использует цвета для выделения проблемных мест:

| Цвет | Значение |
|------|----------|
| 🟢 **Зеленый** | Нормальное время выполнения |
| 🟡 **Желтый** | Время выше среднего, стоит обратить внимание |
| 🟠 **Оранжевый** | Значительное время, возможна оптимизация |
| 🔴 **Красный** | Узкое место, требует оптимизации |

### Exclusive vs Inclusive время

Понимание разницы критически важно:

```
Node A (inclusive: 100ms)
  └── Node B (inclusive: 80ms, exclusive: 80ms)

Node A exclusive = 100ms - 80ms = 20ms
```

**Exclusive** показывает, сколько времени потратил именно этот узел, без учета дочерних операций. Это ключевой показатель для поиска узких мест.

## Пример анализа

### Проблемный план

После загрузки плана в depesz вы увидите что-то вроде:

```
| exclusive | inclusive | rows x |   rows   | loops | node                              |
|-----------|-----------|--------|----------|-------|-----------------------------------|
|     0.033 |    89.456 |    1.0 |      100 |     1 | Limit                             |
|     0.025 |    89.145 |    1.0 |      100 |     1 | → Sort                            |
|    21.777 |    87.567 |    2.5 |     2500 |     1 | → HashAggregate                   |
|    30.555 |    65.789 |    2.25|    45000 |     1 | → Hash Join                       |
|    35.234 |    35.234 |    2.25|    45000 |     1 | → Seq Scan on orders              |
|     1.234 |     2.124 |    1.0 |     2000 |     1 | → Hash (Seq Scan on customers)    |
```

### На что обратить внимание

1. **Seq Scan on orders (35ms exclusive)** - самая долгая операция
   - Возможно, нужен индекс на `created_at`
   - rows x = 2.25 означает, что вернулось в 2.25 раза больше строк, чем ожидалось

2. **Hash Join (30ms exclusive)** - значительное время на соединение
   - Проверьте наличие индекса на `customer_id`

3. **HashAggregate (21ms exclusive)** - группировка
   - Нормально для такого объема данных

## Полезные функции

### Сохранение и шаринг

После отправки плана вы получаете уникальный URL:
```
https://explain.depesz.com/s/ABC123
```

Этот URL можно отправить коллегам для совместного анализа.

### Сравнение планов

Можно открыть несколько планов в разных вкладках и сравнить:
- До и после добавления индекса
- Разные варианты переписывания запроса
- Планы на разных серверах

### Опции отображения

- **Show plan** - показать исходный текст плана
- **Stats** - общая статистика по плану
- **Settings** - настройки отображения

## Пример использования для оптимизации

### До оптимизации

```sql
-- Медленный запрос
EXPLAIN ANALYZE
SELECT * FROM orders WHERE created_at::date = '2024-01-15';
```

depesz покажет красным Seq Scan из-за приведения типа.

### После оптимизации

```sql
-- Быстрый запрос
EXPLAIN ANALYZE
SELECT * FROM orders
WHERE created_at >= '2024-01-15' AND created_at < '2024-01-16';
```

depesz покажет зеленым Index Scan.

## Ограничения

- **Публичный доступ** - планы видны всем, кто знает URL (не загружайте конфиденциальные данные)
- **Только анализ** - не может изменять запросы или давать рекомендации
- **Нет истории** - старые планы могут быть удалены через некоторое время

## Альтернативы

Если нужна приватность, можно развернуть собственный экземпляр:
- GitHub: https://github.com/depesz/explain.depesz.com
- Установка через Docker или напрямую

## Ссылки

- [explain.depesz.com](https://explain.depesz.com/)
- [Блог depesz](https://www.depesz.com/)
- [GitHub репозиторий](https://github.com/depesz/explain.depesz.com)
